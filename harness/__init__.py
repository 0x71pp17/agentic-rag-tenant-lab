"""Measurement: sweeps the attack x configuration space and reports ASR.

`runner.py` executes trials. `report.py` renders the matrix and refuses to
label scripted-backend runs as model measurements.
"""
